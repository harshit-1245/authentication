// const jwt = require('jsonwebtoken');
// const User=require('../models/userModels')

// const isAuthenticated = (req, res, next) => {
//   // Check if the user is authenticated (Token-based)
//   try {
    
//   } catch (error) {
    
//   }
// };

// module.exports = isAuthenticated;
